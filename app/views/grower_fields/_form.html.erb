<%= form_with(model: grower_field, local: true) do |form| %>
  <% if grower_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grower_field.errors.count, "error") %> prohibited this grower_field from being saved:</h2>

      <ul>
      <% grower_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :grower_id %>
    <%= form.number_field :grower_id, id: :grower_field_grower_id %>
  </div>

  <div class="field">
    <%= form.label :field_id %>
    <%= form.number_field :field_id, id: :grower_field_field_id %>
  </div>

  <div class="field">
    <%= form.label :assign_date %>
    <%= form.date_select :assign_date, id: :grower_field_assign_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
